# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MessengerClone' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Giphy', :podspec => 'https://s3.amazonaws.com/sdk.mobile.giphy.com/SDK/2.2.13/Giphy.podspec'

  # Pods for MessengerClone
  target 'MessengerCloneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MessengerCloneUITests' do
    # Pods for testing
  end

end
